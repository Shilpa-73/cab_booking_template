{
  "name": "webpack_demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "yarn run build && node dist/index.js",
    "webpack:build": "rm -rf dist && webpack --config webpack.config.js",
    "webpack:start": "yarn run webpack:build && node dist/index.js"
  },
  "dependencies": {
    "babel-loader": "^8.2.2",
    "dotenv": "^9.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/core": "^7.14.0",
    "@babel/preset-env": "^7.14.1",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^3.0.2",
    "webpack": "^5.36.2",
    "webpack-cli": "^4.7.0"
  }
}
